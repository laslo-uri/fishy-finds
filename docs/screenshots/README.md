# Screenshots

Captured from a live run (`Postgres` + app on `:8080`).

Authenticated pages use `dev-session.html` (signs in as a seed user, then redirects).

```powershell
.\scripts\capture-screenshots.ps1
```

Full API/role smoke:

```powershell
.\scripts\full-smoke.ps1
```

Seed password: `password`.
